# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Vite
.vite/
vite.config.js.timestamp-*

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Local backups (not needed in repo)
.backup-polish/

# Bind mount data (DB + uploads live on the host, not in git)
data/

# Local dev SQLite — produced when running `node server/index.js` outside
# Docker. The real DB lives in the bind-mount above.
server/notetrace.db
server/notetrace.db-shm
server/notetrace.db-wal
server/uploads/

# Editor
.vscode/
.idea/
*.swp
*.swo

# Android — build artifacts and generated files (keep source, ignore generated)
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/capacitor-cordova-android-plugins/
android/.idea/
*.apk
*.aab
*.keystore
android/keystore.properties
# Capacitor sync outputs — regenerated by `npx cap sync android`
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# Generated by scripts/sync-smoke/prepare.mjs
scripts/sync-smoke/.build/
